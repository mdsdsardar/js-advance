let isLoggedIn = true;
if (isLoggedIn) {   //JS will automatically check the var. value.
  console.log("User is not logged in!");
}
let username = "Saad"; //JS will check and consider not empty even for black space.
if (username) {
  console.log("string is not empty");
}
